Recursion is when a method calls itself to solve smaller instances of the same problem.
When a problem can be broken into subproblems of the same kind.
Examples: Factorials, Fibonacci, tree traversal, financial growth.